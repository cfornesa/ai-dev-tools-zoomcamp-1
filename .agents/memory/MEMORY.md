- [Authoring camera and sandbox contract](authoring-camera-and-sandbox-contract.md) — nullable `ArtPieceVersion.camera_placement` preserves legacy overlay behavior across API/viewer/export surfaces; generated previews remain opaque-origin `allow-scripts` sandboxes, with camera and hand tracking owned by the trusted parent bridge.
- [JSON Schema properties open-world semantics](json-schema-properties-open-world.md) — a bare `properties` constraint in an `if` block is vacuously true when the keyed property is absent; always pair with `required` when the condition must trigger on property presence. Fixed in #508's image shape `altText`/`decorative` conditional.
- [E2E spec drift outside the smoke suite](e2e-spec-drift-outside-smoke-suite.md) — non-smoke Playwright specs can hang/fail silently for months after a UI refactor (e.g. #427 moving controls behind a stage popover, #572 changing `/`'s redirect target); actually run a file before trusting it, don't just `--list` it. See #127/#427/#594/#595.
- [Manual admin QA fixture gotchas](manual-admin-qa-fixture-gotchas.md) — `reconcile_admin_identities` silently revokes the `e2e_admin` fixture's grant (not in `ADMIN_IDENTITIES`), and re-running `e2e_fixtures create` mid-session invalidates any open browser login.
- [Replit Development migrations not auto-applied](replit-development-migrations-not-auto-applied.md) — CONFIRMED #589: Development's own django_migrations can fall behind (stopped at 0077, missing 0078), not just Production; Publish's schema-diff then finds nothing to propagate no matter how many times it runs. Check Dev's ledger first, not just Prod's.

- [Portable orchestration dispatch profiles](portable-orchestration-dispatch-profiles.md) — use Claude Sonnet Medium by default for `task-distillation` and `backlog-session`; Codex Luna Medium and Antigravity Gemini 3.8 Flash/Sonnet are supported alternatives, not substitutions, and `DISPATCH.md` remains a contextual dispatch registry rather than a prompt catalog.
- [Cross-repo parity inventory](cross-repo-parity-inventory.md) — the Django/React app has narrow admin settings and entitlement foundations; broader CMS pages/content operations, public profiles, and site/profile theme parity must be ported as separate contracts from the read-only augment-humankind references, while preserving local-first and opaque-origin boundaries.
- [Implementation source priority and Creatrweb hosting](implementation-source-priority-and-creatrweb-hosting.md) — use `augment-humankind-react-node` for React implementation patterns and `augment-humankind` for functional behavior across pieces, sharing, and prompt-to-product/output flows; `creatrweb` is the Replit host for this app and must be Git-synced to the reviewed `origin/main` commit before publishing.
- [Full matrix is scheduled, not manual](full-matrix-scheduled-not-manual.md) — don't manually re-trigger the ~40-minute full multi-browser CI matrix after every batch; it already runs nightly via cron, and repeated manual triggers can cancel each other on a shared concurrency group.
- [Replit Google Frontend header rewriting](replit-google-frontend-header-rewriting.md) — the published domain's edge duplicates HSTS (#490) and downgrades Cache-Control public→private via an injected Set-Cookie (#489); both are platform boundaries, not code defects.
- [uv run --directory is unreliable on Replit runtime](uv-run-directory-flag-unreliable-on-replit.md) — use `(cd X && uv run ...)` subshells instead; `--directory` intermittently left the subprocess cwd unchanged, resolving manage.py against a stale pre-restructure root .venv. See #249.
- [Stop goal loop when blocked on Replit publish](stop-goal-loop-when-blocked-on-replit-publish.md) — a Replit publish blocker stops only dependent work; continue independent closure-ready issues and halt only when no independent work remains. Non-user-judgment dependency/environment blockers require a fresh distillation reconciliation before the next issue.
- [3D rendering: npm dependency vs CDN sandbox](3d-rendering-npm-dependency-vs-cdn-sandbox.md) — untrusted AI-generated 3D code uses the CDN+iframe sandbox; the editor's own schema-validated preview uses a real npm dependency (Three.js, like p5). Don't conflate the two. See #244.
- [E2E fixtures new-project-family checklist](e2e-fixtures-new-project-family-checklist.md) — a new PROTECT-FK/soft-delete model needs e2e_fixtures.py cleanup updated and class-based e2e locators checked before its first E2E spec lands. See #239/#242.
- [Replit schema-diff gap for new tables](replit-schema-diff-gap-for-new-tables.md) — CONFIRMED: Replit Publish skipped brand-new FK tables (scenes_project3d etc); django_migrations never recorded the migration. Fix is a Republish, not a direct migration. See #238.
- [Replit dashboard is browsable via Claude in Chrome](replit-dashboard-browsable-via-claude-in-chrome.md) — when the user's Chrome session is signed into Replit, an agent can navigate replit.com/@owner/project directly to read Logs and the Database panel (actual tables/row counts), not just the published app.
- [Claude Code blocks database writes via browser](claude-code-blocks-database-write-via-browser.md) — the auto-mode classifier hard-blocks typing write SQL/config values into Replit's dashboard even with explicit user authorization; hand the exact statement to the user to run themselves instead of retrying.
- [PostgreSQL migration SQL](postgresql-migration-sql.md) — Raw PL/pgSQL in Django migrations must escape literal percent signs for Psycopg 3.
- [Vitest shell file paths](vitest-shell-file-paths.md) — Read static shell files from the frontend process root because transformed import.meta URLs are not file URLs.
- [Playwright runtime prerequisites](playwright-runtime-prerequisites.md) — Browser E2E needs Chromium system libraries and the project fixture environment.
- [E2E wrong Docker project](e2e-wrong-docker-project.md) — A healthy unrelated Compose stack can serve the expected port while health/API probes return the wrong app.
- [Replit publish verification](replit-publish-verification.md) — Use deployment-status events for checks that must run after publishing; direct production table checks are required, and production fixture imports need the explicit owner-scoped workflow in #633.
- [Replit detached checkout publish verification](replit-detached-checkout-publish-verification.md) — A stale Git-panel authentication/rebase message can coexist with the correct detached shell revision; verify `HEAD`/`origin/main` in the shell before any destructive recovery, then republish and verify the live canonical route.
- [Replit development Shell versus production import](replit-development-shell-vs-production-import.md) — The interactive Replit Shell targets Development Database; published augmentrart.com reads Production Database. Never treat Shell importer output as production evidence; confirm the production runtime/database before importing and recheck the live API.
- [Mistral credential rotation](mistral-credential-rotation.md) — Retain prior Fernet roots during controlled credential re-encryption before retiring them.
- [Allauth JWT runtime dependency](allauth-jwt-runtime-dependency.md) — Pin PyJWT explicitly because the installed allauth release imports jwt during OAuth callback validation without declaring it.
- [Browser-facing CSRF origin](browser-facing-csrf-origin.md) — Same-origin browser POSTs must trust the Vite origin, not only Django's internal backend port.
- [GitHub HTTPS credential helper](github-https-credential-helper.md) — A valid GIT_URL secret may need an explicit one-command Git credential helper to bypass stale HTTPS credentials.
- [Published-app checkpoint commits](published-app-checkpoint-commits.md) — Treat “Published your App” commits as disposable checkpoints during Git reconciliation; preserve meaningful ancestor work and drop checkpoint-only divergence.
- [Ignored tracked task files](ignored-tracked-task-files.md) — Some tracked .local task files are ignored by default; force-stage the intended incoming version when resolving modify/delete conflicts.
- [Replit production schema publishing](replit-production-schema-publishing.md) — Replit applies production schema diffs during Publish; Django migrations must stay out of build and startup commands. #598: a single RunPython-then-schema ledger gap blocks *every* future migrate call, and a table existing doesn't mean its RunPython data effect actually ran.
- [GitHub Actions needs failure gate](github-actions-needs-failure-gate.md) — dependent alert jobs need an explicit `always()` gate before checking a failed prerequisite result.
- [Critical operational decisions](critical-actions.md) — Structured index of the security, deployment, database, authentication, and verification decisions that must not regress.
- [p5.js getUserMedia polyfill](p5-getusermedia-polyfill.md) — Mocking navigator.mediaDevices.getUserMedia wrong crashes the bundle via p5.js's own load-time polyfill.
- [Local e2e baseURL default](e2e-baseurl-port-default.md) — RESOLVED: Playwright's default baseURL now matches Vite's pinned :5000; historical note on issue #123.
- [jsdom drag-event clientY](jsdom-drag-event-clientY.md) — `fireEvent.drag*` silently drops `clientY`/`clientX` in jsdom; build the event by hand and fire it with `fireEvent(el, event)` instead.
- [Replit userenv scope](replit-userenv-scope.md) — `[userenv]` is workspace-scoped, not deployment-authoritative; `[userenv.production]` now pins production-safe values as defense-in-depth.
- [AI feature daily quota exhaustible by retesting](ai-feature-daily-quota-exhaustible-by-retesting.md) — repeatedly live-retesting an AI generation feature can itself exhaust the account's daily quota; space checks out, don't poll on every tick.
- [Replit production frontend serving](replit-production-frontend-serving.md) — The published deployment ran Vite's dev server (live HMR) in production until issue #133; verify live, not just from source.
- [PostgreSQL multi-db test pitfalls](postgres-multi-db-test-pitfalls.md) — RunPython migrations need `.using(alias)`; threaded postgres_test tests need `default` aliased onto the same DB; plain `RAISE EXCEPTION` maps to `ProgrammingError`.
- [Local port 8000 Docker conflict](local-port-8000-docker-conflict.md) — RESOLVED: vite.config.ts now defaults its proxy target to 127.0.0.1:8000, not localhost:8000, fixing the silent IPv6/Docker collision for good.
- [Code tab mount-once staleness](code-tab-mount-once-staleness.md) — The editor's Code tab only resyncs on mount, so Undo/Redo and a bare Visual<->Code toggle can show stale content or silently discard unsaved edits (#177).
- [Local sandbox verification boundaries](local-sandbox-verification-boundaries.md) — Managed macOS runs may block uv cache writes, launcher subprocesses, and loopback socket binds; rerun full gates in an approved environment.
- [Local-first project media architecture](local-first-project-media-architecture.md) — New projects default to browser-local scenes and media; optional cloud sync is explicit backup and paid eligibility must never revoke local creative work. See #507/#509/#511.
- [Cloud media retention policy](cloud-retention-policy.md) — #522 owner-approved 30-day remote-copy grace periods for deleted, expired, and disabled-sync states; active copies remain, local IndexedDB is never purged, and retroactive deletion requires explicit admin confirmation.
- [Full browser readiness gate](full-browser-readiness-gate.md) — Targeted browser suites do not establish project readiness; the complete disposable-stack browser command must pass before release.
- [Layer/visual selection contract](layer-visual-selection-contract.md) — Layer and canvas selection must be bidirectional, and the owning layer's entire block must receive selected treatment.
- [Camera synthetic verification gap](camera-synthetic-verification-gap.md) — A camera-performance issue was closed QA:PASS 3x on synthetic mocked-frame evidence while production stayed broken; require real-camera evidence before closing this class. Extends to microphone (#479): camera/mic are separate macOS TCC grants, one can pass while the other hangs on the same host.
- [Multi-library generation architecture fork](multi-library-generation-architecture-fork.md) — Multi-library AI art generation forces a schema-vs-raw-code architecture choice that gates issues #199/#200; decide in #197 first.
- [Replit autoscale startup wait race](replit-autoscale-startup-wait-race.md) — A trap/wait race in scripts/start.sh could log a false "exited with status 127" on an ordinary autoscale stop; fixed in #202.
- [Mistral non-strict schema mode](mistral-non-strict-schema-mode.md) — response_format uses strict:False; restate enums, per-type required fields, AND cross-item constraints (layerId uniqueness, not schema-expressible at all) for EVERY array-of-typed-object field, 2D and 3D alike — partial restatement (e.g. objects[] but not lights[]) still leaves a gap. Fixed for 2D shapes in #256; 2D layerId/demoSignals open in #264; 3D lights open in #265; additive Personas (#260) must stay a separate system message.
- [Structured editor multi-renderer scoping](structured-editor-multi-renderer-scoping.md) — Scoping for extending the main editor (not just AI art-pieces) to Canvas2D/SVG renderers; #206/#207/#208 phased under epic #205.
- [Mistral SDK import path](mistralai-sdk-import-path.md) — `mistralai` has no top-level `Mistral`/`MistralError`; import from `mistralai.client`. Caused #203's production 500.
- [E2E draft-seed client_seq race](e2e-draft-seed-client-seq-race.md) — an e2e test seeding a server draft with a small hardcoded `client_seq` can lose a race against the app's own periodic autosave; use a very large value instead. Caused #193's recurring flake.
- [Scene schema allOf branch property shadowing](scene-schema-allof-branch-property-shadowing.md) — a base `shape` property not re-listed in every type-specific `allOf` branch's closed allowlist is silently rejected for every shape. Caused #214's AI create-scene 422s.
- [Feature branches with completed work not merged to main](feature-branches-completed-work-not-merged-to-main.md) — tasks.md/GitHub can say COMPLETE/closed while the code only exists on an unmerged branch; verify before building on it. Found for #210-213.
- [A-Frame default camera facing convention](aframe-default-camera-facing-convention.md) — a camera at negative Z with no rotation faces away from origin-centered content; a flat shape laid down as a floor is edge-on to it either way. Two of #236's three root causes.
- [Pinned CDN version can silently 404](pinned-cdn-version-can-silently-404.md) — aframe@1.5.0's jsdelivr package has no `dist/aframe.min.js`; #236's real blocker after both prompt fixes. Check the pinned URL's HTTP status before re-auditing AI prompts.
- [CSP-blocked eval masquerades as library bug](csp-blocked-eval-masquerades-as-library-bug.md) — A-Frame needs `'unsafe-eval'` in the CSP or it throws a cryptic unrelated `TypeError`; cross-origin script errors also need `crossorigin="anonymous"` to unmask. #236's 4th and final root cause.
- [Generated art-piece surface parity](generated-art-piece-surface-parity.md) — The original PHP reference is locally available at `../augment-humankind`; use its maintained parity contract and runtime/admin source when grooming generated-piece work. Generated pieces must remain sandboxed and coordinate regular, embed, immersive, and ZIP surfaces through one capability contract. See #313-#319.
- [Authored piece surface parity](authored-piece-surface-parity.md) — The deployed `/p/:id`, `/p3d/:id`, and `/projects3d/:id` examples are the structured editor's authored-piece surfaces, distinct from `/art-pieces/*`; parity must include shared overlays, packaged download runtimes, and per-consumer inner-toolbar layout checks. #274/#320/#324 are immutable reconciliation containers; the current atomic matrix is recorded in `docs/tasks.md`, with #355/#356 owning the current deployment boundaries.
- [Scoped closure interpretation](authored-piece-surface-parity.md) — A prior issue closure remains valid for its transaction's scoped contract and evidence. Later owner reports are new evidence; they do not retroactively invalidate or reopen closed work without explicit authorization naming that issue.
- [Parity closure evidence gap](parity-closure-evidence-gap.md) — Local, localhost, disposable-stack, source-string, or pushed-commit evidence cannot close deployed route or extracted-download criteria; verify the exact deployed asset/surface independently, inspect rendered screenshots for visual criteria, and re-distill after non-user-judgment blockers.
- [Stage command overlay browser checks](stage-command-overlay-browser-checks.md) — scope state-changing trigger locators and dialog groups to their current overlay structure; mobile submenus must stay within the stage.
- [WebKit local runtime dependencies](webkit-local-runtime-dependencies.md) — Playwright WebKit may need distro-matched ABI libraries locally; CI's `install --with-deps` remains the authoritative browser gate.
- [Fullscreen browser Escape dialog state](fullscreen-browser-escape-dialog-state.md) — Firefox/WebKit can retain the stage command dialog after Escape exits fullscreen; keep Chromium's hidden assertion strict.
- [Responsive 3D preview proportions](responsive-3d-preview-proportions.md) — Keep renderer/camera/frame dimensions aligned so uniform spheres stay round; preserve intentional object transforms and verify mobile overflow/clipping. See #349/#348.
- [Owner-reported parity correction queue](authored-piece-surface-parity.md) — Closed issues remain immutable; later rendered/deployed contradictions become new criterion-ready linked issues. #376–#378 are permanently closed; #379 is blocked by the Replit synchronization defect tracked in #380. Full parity remains limited to augment-humankind pieces implementation/examples translated to Django/Python plus React/TypeScript.
- [Public gallery published-piece boundary](public-gallery-published-piece-boundary.md) — The header `/gallery` currently lists only structured 2D `Project` rows; published `Project3D` rows lack a gallery-list contract. Generated `ArtPiece` rows remain on separate `/art-pieces/gallery`. Issue #392 owns the mixed structured 2D/3D listing.
- [Multi-vendor AI provider credentials](multi-vendor-ai-provider-credentials.md) — Preserve shared validated scene contracts and encrypted owner-scoped keys when adding Gemini/DeepSeek; Mistral Vibe remains developer tooling unless scope changes.
- [Draw.io layer integration](drawio-layer-integration.md) — Keep draw.io as a separately versioned object document inside the native outer layer stack; do not flatten it into native one-shape-per-layer content.
- [Production readiness gaps](production-readiness-gaps.md) — AI quotas need shared production state, published runtime needs WSGI/ASGI, and signup policy must be explicit across plan and UI.
- [Auth, admin, and entitlement boundaries](auth-admin-entitlements-boundaries.md) — GitHub OAuth/admin identities/entitlements/admin settings/PayPal billing/account identity link-unlink (#420-#426, all closed 2026-09-05); key design decisions for anything touching this boundary next.
- [Replit migrations ledger not updated by publish](replit-migrations-ledger-not-updated-by-publish.md) — CONFIRMED: Replit's schema sync creates real tables/columns but never writes to django_migrations; verify with /health/ + direct table checks, never the migrations ledger. A retry-Publish with no code changes did fix it this time.
- [WebKit MediaDevices instance instability](webkit-mediadevices-instance-instability.md) — CONFIRMED: in a sandboxed opaque-origin iframe, WebKit hands out a fresh navigator.mediaDevices instance per access; mock getUserMedia by patching MediaDevices.prototype, never the instance. See #454.
- [Agentic AI editing boundary](agentic-ai-editing-boundary.md) — reuse validated provider/proposal services with bounded owner-scoped runs, explicit Accept, cancellation and quota accounting; keep raw generated code outside the agent tool surface.
- [Prescriptive-dispatch artifacts](subagent-dispatch-artifacts.md) — subagent-inserted code can carry shell-escaping artifacts (a literal `\$` regex 404'd silently at #494) and dispatch prompts can quote stale current state; verify dispatched edits byte-level and at runtime, never trust typecheck/lint green alone.
- [Recovery link accessible-name collision](recovery-link-accessible-name-collision.md) — a page nested in `Layout` must not reuse the shell nav's bare "Home"/"Public gallery" link text, or desktop-width role queries hit ambiguous duplicates. See #485.
- [E2E global fixture isolation](e2e-global-fixture-isolation.md) — fixture users' server-side sessions and singleton settings (SiteSettings/Plan) need explicit per-file reset, unlike per-project resources. Session-count assertions drift without it; hardcoded baselines drift from seeded defaults. See #505.
- [Live published audit follow-ups](live-published-audit-followups.md) — the 2026-09-17 audit found healthy JS/CSS delivery but production `seo_config` schema drift, invalid `metadata_tags`, SPA interception of llms routes, and UI follow-ups #589-#594; use the backlog entry and repair #589 first.
- [E2E full-matrix Firefox iframe timeouts](e2e-full-matrix-firefox-iframe-timeouts.md) — headless Firefox on the CI runner cannot create any WebGL context inside the `sandbox="allow-scripts"` iframe (trace: "Exhausted GL driver options"), so Three.js/A-Frame sandboxes never start and `waitForThreeJsReady`/`sendCommandAndAwaitPose` wait forever at any timeout. ROOT-CAUSED: permanent platform boundary, not slowness/defect; the 3 WebGL scenarios skip on firefox. See #506.
- [Disposable Docker browser source sync](docker-compose-browser-source-sync.md) — Compose images are not source-mounted; use `E2E_DOCKER_COMPOSE=true` and, only for disposable local QA when rebuild pulls fail, copy reviewed source into the containers. This is never production evidence.
- [C2 opaque sandbox rendering](c2-opaque-sandbox-rendering.md) — upstream c2.js can expose a global yet fail inside the opaque `srcdoc`; preserve the reference `{ c2, canvas, startFrame }` contract with a local Canvas2D adapter, synchronous first frame, and 320x240 viewport.
- [Flat-engine immersive presentation](immersive-flat-engine-presentation.md) — SVG, p5.js, and C2.js immersive views use the lazy synthetic spatial shell and require a presentation-specific responsive canvas/SVG surface; immersive support remains separate from embed/download/editor capability flags.
- [Canonical piece route test contract](canonical-piece-route-test-contract.md) — New profile/gallery/studio/editor/immersive assertions target slug routes, while UUID paths remain compatibility shims; wait for controlled profile data before editing and scope fixture selectors.
- [Canonical slug allocation and soft deletion](canonical-slug-allocation-soft-delete.md) — Public-slug collision checks must use each piece model's unfiltered manager because soft-deleted rows remain protected by `(owner, public_slug)` uniqueness constraints.
- [Account-settings browser contract](account-settings-browser-contract.md) — Anonymous/protected redirects land at `/gallery?type=all`; secondary account-settings sections are collapsed by default and must be explicitly expanded in browser tests.
- [Local PostgreSQL browser verification](local-postgres-browser-verification.md) — when Docker is unavailable but local non-production PostgreSQL is reachable, run Django/Vite directly; retry Playwright outside the managed sandbox when Chromium Mach-port startup is denied, and preserve unrelated full-run failures by issue mapping.
- [Replit final schema publish verification](replit-final-schema-publish-verification.md) — for migration-bearing publishes, reject destructive conflict options, stage nullable schema/backfill before tightening, verify actual production rows as well as tables plus smoke/runtime, and never rely on `django_migrations`.
- [E2E fixture state and skipped tests](e2e-fixture-state-and-skipped-tests.md) — a self-skipped Playwright scenario is prerequisite evidence, not feature evidence; verify fixture setup without exposing credentials and clean up manually seeded fixtures.
- [PayPal event replay idempotency](paypal-event-replay-idempotency.md) — a previously rejected signed event is not reprocessed under the same provider event ID; use a genuinely new provider delivery for post-fix external evidence.
- [LinkedIn OIDC callback registration](linkedin-oidc-callback-registration.md) — generic allauth OIDC uses `/accounts/oidc/<provider_id>/login/callback/`, including the trailing slash; LinkedIn rejects legacy callback paths before consent.
- [Account settings production audit 2026-09-15](account-settings-production-audit-2026-09-15.md) — live entitlements 500 (#547) and flat-column layout gap (#548) found on augmentrart.com; admin panel exists with closed-scope theme tokens, richer CSS theming is an intentional #521 exclusion.
- [Server-authoritative sync conflict resolution](server-authoritative-sync-conflict-resolution.md) — lock the project row directly before comparing versioned scene state; nullable joined relations cannot be selected for update on PostgreSQL, and stale bases must return authoritative context without writing.
- [Replit final schema publish verification](replit-final-schema-publish-verification.md) — a successful local migration or Replit “Published your app” status is not production schema evidence; direct table checks, smoke, and deployed-browser verification are required, and missing migration tables remain a release blocker.
- [Login flake outlives timeout fix](login-flake-outlives-timeout-fix.md) — loginViaUI still intermittently times out after #492's 15s timeout widening; recurred on two more specs/viewports in 2026-09-16's readiness pass, filed as #549 rather than reopening #492.
- [CSS hidden attribute silently overridden](css-hidden-attribute-silently-overridden.md) — a same-specificity author CSS rule can defeat `hidden` at every viewport, not just its own media query; verify with `getComputedStyle`, not DOM presence. Caused #584.
- [QA batch untested handoffs](qa-batch-untested-handoffs.md) — a stage-2 handoff's "N tests passed" often means pre-existing regression tests, not new coverage of the issue's own criteria; diff the test file against the criteria list. Found across #571-#587.
- [Design/share parity gaps](design-share-parity-gaps.md) — generated thumbnails are upload-only (always fallback), OG tags are client-JS only, no theme toggle/Pareto/Celestial; issues #642–#655.
- [Production HTML missing share metadata](production-html-missing-share-metadata.md) — published HTML had no og:/feed-alternate tags while APIs worked; bundle hash ≠ vite.config.ts identity; see #700.
- [Production share-metadata backend forwarding](production-share-metadata-backend-forwarding.md) — with Django HTTPS redirects enabled, Vite's internal production fetches need forwarded HTTPS headers while targeting the local HTTP backend; verify `backend_reachable:true` with the post-publish smoke check.
- [Production data-action gap in self-QA](production-data-action-gap-in-self-qa.md) — a self-QA'd "terminal-ready locally" fix can be code-correct yet fail its own criterion live when it depends on a data row or one-time management command never run against production; verify the actual live response, don't reopen/re-engineer, file a scoped data-action follow-up. See #712/#716/#722/#723.
- [Generated ZIP export parity gaps](generated-zip-export-parity-gaps.md) — ZIPs lack drawing toolset/styled controls and stub c2; "no controls" report unresolved until an extracted ZIP is screenshotted. See #751-#762.
- [Contextual, focused workspace UX](contextual-focused-workspace-ux.md) — owner wants contextual, progressively disclosed editor controls in familiar menus; add a "no unrelated controls" screenshot check to editor issues.
