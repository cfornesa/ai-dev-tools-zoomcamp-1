# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (never commit secrets; .env.example stays tracked)
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright (Task 65, issue #65)
/playwright-report
/test-results
/e2e/.e2e-state.json

# Runtime-limits benchmark output (Task 70, issue #70) -- regenerated by
# `npm run bench:runtime` on every run; _docs/benchmarks.md documents a
# reference run's numbers instead of committing this machine-specific,
# repeatedly-regenerated file.
/e2e/benchmark/results/
